{
  "name": "code-scanning-dev",
  "private": true,
  "type": "module",
  "description": "Developer tooling only. The skill itself has no dependencies.",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "22.20.2",
    "typescript": "7.0.2"
  }
}
