{
  "name": "vuln-app",
  "version": "0.0.0",
  "private": true,
  "description": "Deliberately vulnerable fixture. Never deploy. Used to produce real scanner output.",
  "main": "src/app.js",
  "scripts": { "start": "node src/app.js", "test": "node test/smoke.test.js" }
}
